{
  "name": "vaultbox-api",
  "version": "2.1.0",
  "description": "Secure cloud storage API with resumable chunked uploads, range downloads, atomic quota enforcement, Redis coordination, audit logs, and administrative controls.",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "npx --yes prisma@6.12.0 migrate deploy && node src/server.js",
    "build": "echo build skipped",
    "lint": "npx --yes eslint@10.10.0 src scripts test prisma",
    "test": "node --test",
    "benchmark": "node scripts/benchmark.js",
    "deploy:migrate": "npx prisma migrate deploy",
    "seed": "node prisma/seed.js"
  },
  "keywords": [
    "cloud-storage",
    "file-storage",
    "fastify",
    "prisma",
    "postgresql",
    "redis",
    "jwt",
    "swagger",
    "api",
    "saas",
    "resumable-downloads",
    "resumable-uploads",
    "chunked-uploads",
    "http-range",
    "parallel-downloads"
  ],
  "author": "Williams Ashibuogwu",
  "license": "MIT",
  "engines": {
    "node": "24.x",
    "npm": ">=11 <12"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/jwt": "^10.2.2",
    "@fastify/multipart": "^10.1.1",
    "@fastify/rate-limit": "^11.2.0",
    "@fastify/swagger": "^9.8.1",
    "@fastify/swagger-ui": "^6.1.1",
    "@prisma/client": "6.12.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "fastify": "^5.12.1",
    "redis": "^6.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "prisma": "6.12.0"
  }
}
